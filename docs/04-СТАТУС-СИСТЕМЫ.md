# Статус системы

Актуально на: **2026-02-17**

## 1) Состояние данных
- Рабочая файловая БД переведена на схему `json-v2`.
- Версия схемы: `data/_meta/schema-version.json` (`version: 2`).
- Операционные директории в чистом состоянии:
  - `data/wash-events`: `0` файлов
  - `data/expenses`: `0` файлов
  - `data/client-transactions`: `0` файлов
  - `data/employee-transactions`: `0` файлов
- Остаток химии: `0` (`data/inventory.json` -> `chemicalStockGrams: 0`).
- Балансы контрагентов/агрегаторов приведены к числовому состоянию (текущее значение: `0`).

## 2) Миграция JSON v2
- Добавлен контур миграций: `scripts/migrations/json-v2/*`.
- Прогон:
  - `dry-run`: успешно, без критических ошибок.
  - `apply`: успешно, создан бэкап и миграционный лог.
  - повторный `apply`: изменений в рабочих JSON нет (идемпотентность подтверждена).
- Артефакты:
  - `data-backups/20260214-214837`
  - `data-backups/20260214-214845`
  - `data/_meta/migration-log-20260214-214837.json`
  - `data/_meta/migration-log-20260214-214845.json`

## 3) Запуск и инфраструктура
- `START.ps1` больше не форсирует Go backend.
- Режим по умолчанию: `USE_GO_BACKEND=false` (Next API).
- Go backend запускается только при явном `USE_GO_BACKEND=true`.
- Добавлены эксплуатационные скрипты:
  - `scripts/rebuild-safe.ps1`
  - расширенный `scripts/cleanup-safe.ps1`
  - обновленный `scripts/health-check.ps1` (условный backend health check)

## 4) Проверки (2026-02-16)
- `npm run typecheck`: **OK** (0 ошибок).
- `npm run build`: OK при чистом прогоне после `STOP.ps1` + `scripts/cleanup-safe.ps1`.
- Предупреждения сборки (некритичные): `opentelemetry`, `browserslist`.
- Telegram bot: `TELEGRAM_BOT_ENABLED=false npm run bot:telegram` → OK (graceful exit).

## 5) UX-улучшения графика и планирования (2026-02-17)
- **11 улучшений** в 3 файлах (подробности в `03-ЖУРНАЛ-РАБОТ.md`).
- Подсветка "сегодня" в календаре менеджера и экране сотрудника.
- Спиннеры, подтверждения удалений, поиск сотрудников в диалоге.
- Строка итогов по дням в таблице графика.
- Баннер "Ближайшая смена" для сотрудника.
- Защита от случайной очистки смен при автозаполнении.

## 6) Исправлены проблемы (2026-02-16)
- **16 багов** исправлены в 10 файлах (подробности в `03-ЖУРНАЛ-РАБОТ.md`).
- Ключевые:
  - Timing attack в Telegram auth → `crypto.timingSafeEqual()`.
  - Неправильный вес канистры (20кг вместо 21кг из настроек) → читается из `inventory.settings`.
  - Крэши рабочей станции при отсутствии машин/прайсов у агрегаторов → optional chaining.
  - Клонирование графика по индексу → по дню недели.
  - Зависание Telegram-бота → таймаут 15с.
  - Некорректная валидация в 3 API-эндпоинтах.

## 6) Риски
- Риск безопасности: аутентификация через cookie `employee_auth_sim` — cookie содержит данные пользователя, которым сервер доверяет напрямую. Рекомендуется переход на подписанные сессии (JWT или server-side session) в будущем.
- Риск безопасности: Telegram токен хранится в `telegram-bot/.env.local`.
- Риск совместимости режима `USE_GO_BACKEND=true`:
  - часть полей и проверок в Go backend может отставать от Next API-контрактов.
- Риск очистки артефактов в WSL:
  - phantom-entry `/.next-runtime*/trace` может давать `EPERM`.

## 7) Следующий шаг
- Выполнить `scripts/rebuild-safe.ps1` из Windows PowerShell.
- Smoke-тест ключевых страниц: `/login`, `/dashboard`, `/employee/workstation`, `/schedule`, `/employees`, `/counter-agents`, `/aggregators`, `/expenses`, `/invoices`.
- Запуск Telegram-бота и проверка команд.

## 8) Планирование смен (погода/паттерны)
- Погодный помощник для требований месяца добавлен в `/schedule/planning/[id]` (режим "Требования по дням").
- Endpoint прогноза: `GET /api/weather/forecast?month=YYYY-MM` (admin-only, локация Вязники).
- Журнал месяца: `GET /api/pattern-journal/month?month=YYYY-MM` (admin-only), сохраняет файл в `data/_meta/pattern-journal`.
